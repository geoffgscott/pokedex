/** Loading status for async data */
export enum LoadingStatus {
  Fetching = 'fetching',
  Idle = 'idle',
  Error = 'error',
  Initial = 'init',
}

export enum PokemonDataTypes {
  Evolution = 'evolution',
  Species = 'species',
}
